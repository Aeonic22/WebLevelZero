# Static Site

This project was created from the current folder and contains a simple static website.

Available scripts:

- `npm start` - Serve files using http-server on port 8080
- `npm serve` - Serve files using serve on port 8080
- `npm preview` - Same as start

Install servers globally or use npx as shown.
